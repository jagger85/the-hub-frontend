import { colors, boxShadow } from "../../../theme"

export const styles = {
    container: {
    },

    transactionContainer:{
        backgroundColor: colors.background[1000],
        boxShadow: boxShadow
        
    }
}