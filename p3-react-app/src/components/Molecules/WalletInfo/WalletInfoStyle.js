import { colors, boxShadow } from '../../../theme';

export const styles = {
  paper: {
    padding: 2,
    backgroundColor: colors.background[1000],
    boxShadow: boxShadow,
    marginBottom: 2
  },
  balances:{
    display: 'flex',
    flexDirection: "column",
    marginBottom: 1,
    marginTop: 1,

  }
};
