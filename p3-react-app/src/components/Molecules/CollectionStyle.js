import {colors, boxShadow} from '../../theme'

export const styles = {
    paper :{
        padding: 1,
        backgroundColor: colors.background[900],
        boxShadow: boxShadow,
        ":hover":{
            border: 1,
        }
    },

        
}