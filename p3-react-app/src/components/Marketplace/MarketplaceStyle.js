import { colors } from "../../theme"

export const styles = {
    marketContainer:{
        margin: 1,
        padding: 1,
        display: 'flex',
        flexWrap:'wrap',
        justifyContent: 'center'
    }
}